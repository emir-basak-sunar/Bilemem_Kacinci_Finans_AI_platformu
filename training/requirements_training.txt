# ============================================================
# FinAI Training Requirements — v2.0
# ============================================================
# Colab A100'de kurulum:
#   pip install -r training/requirements_training.txt
#
# Değişiklikler v2.0:
# - arch eklendi (GARCH volatility model)
# - optuna[storages] eklendi (SQLite persistence)
# - pyarrow eklendi (parquet okuma/yazma hızı)
# - Versiyon pinleri güncellendi
# ============================================================

# ── Core ────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11          # Spearman IC, Ljung-Box, bootstrap CI
pyarrow>=14.0        # Parquet okuma/yazma (pandas backend)

# ── Feature Engineering ─────────────────────────────────────
ta>=0.11             # Teknik indikatörler (RSI, MACD, BB, ATR vb.)

# ── Tree Models ─────────────────────────────────────────────
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# ── Deep Learning ───────────────────────────────────────────
torch>=2.2           # BiLSTM, TCN, TFT modelleri

# ── Statistical Models ──────────────────────────────────────
statsmodels>=0.14    # SARIMAX, Ljung-Box testi
pmdarima>=2.0        # Auto-ARIMA, SARIMA
arch>=6.3            # v2.0: GARCH volatility model (pip install arch)

# ── ONNX Export ─────────────────────────────────────────────
onnx>=1.15
onnxruntime>=1.17    # Serving'de inference
skl2onnx>=1.16       # XGBoost/LightGBM/CatBoost → ONNX

# ── Data Collection ─────────────────────────────────────────
yfinance>=0.2        # OHLCV + makro veri (VIX, S&P500, DXY vb.)

# ── Hyperparameter Optimization ─────────────────────────────
optuna>=3.5          # XGBoost/LightGBM/CatBoost tuning
# optuna-storages: optuna>=3.5 ile birlikte gelir, ayrı kurulum gerekmez

# ── Explainability ──────────────────────────────────────────
shap>=0.44           # SHAP feature importance (XGBoost)

# ── Visualization (opsiyonel, Colab'da kullanılır) ──────────
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# ── Utilities ───────────────────────────────────────────────
joblib>=1.3          # Model kaydetme/yükleme
tqdm>=4.66           # Progress bar
python-dotenv>=1.0   # .env dosyası desteği
